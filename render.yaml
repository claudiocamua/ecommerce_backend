services:
  - type: web
    name: ecommerce-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: DB_NAME
        value: ecommerce
